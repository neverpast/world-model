# World Models 协作规则

在本目录工作的 AI 必须先阅读 `README.md`、`CONSTITUTION.md`、`_system/MODEL_REGISTRY.md` 和相关地图的 `README.md`。

## 每次讨论后的维护流程

1. 判断归属地图；跨领域模型选择机制来源所在地图作为主归属。
2. 判断变更类型：新模型、旧模型更新、新连接、修正、反例。
3. 先搜索已有模型，优先更新、连接或合并，避免创建近义模型。
4. 按 `_system/templates/model.md` 的结构处理内容。
5. 若形成文件变更，同步维护地图索引、模型注册表、连接索引和演化日志。
6. 回复中必须给出“世界地图更新”；没有落盘变化时明确写“无文件更新”以及原因。

## 编辑约束

- 保留用户已有内容；不得为统一格式而静默丢失信息。
- 事实、解释、假设和预测必须可区分。
- 不虚构证据、引用或确定性。
- 可信度变化必须说明证据原因。
- 模型冲突应并列记录，不用措辞折中掩盖真实矛盾。
- 除非用户在当前请求中明确要求，不使用 Superpowers 相关 skills。

