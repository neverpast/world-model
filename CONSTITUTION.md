# 项目宪法

## 使命

World Models 是一个用于解释、预测与决策的长期认知系统，而不是知识仓库。模型是暂时有效的压缩解释，必须持续接受证据、反例与竞争模型的检验。

## 核心原则

1. 不记录知识，建立模型。
2. 不追求静态结论，追求可检验的解释能力。
3. 不迷信权威，区分证据、推断与假设。
4. 所有模型都允许被修正、合并、降级或删除。
5. 每个模型都必须说明适用范围和失效条件。
6. 优先建设跨领域可迁移的模型。
7. 保持命名、结构和索引一致，避免知识碎片化。

## 模型的最低质量门槛

一个正式模型必须同时具备：

- 可观察的现象，而非只有概念定义。
- 能连接原因与结果的底层机制。
- 一句话模型，以及关键变量之间的关系。
- 明示的核心假设、适用范围和失效条件。
- 至少一个可被证伪的预测。
- 可信度等级及其证据理由。
- 与其他模型的支持、冲突或组合关系。

不满足门槛的内容应继续讨论或作为某个模型的证据，不单独创建正式模型。

## 证据纪律

- 明确区分：**观察事实**、**机制解释**、**模型推断**、**价值判断**。
- 可信度表示当前证据状态，不表示模型的重要性。
- 新证据既要寻找支持，也要主动寻找反例和替代解释。
- 权威观点只是一类证据，不自动高于可重复观察和预测结果。
- 无法证伪的陈述不能作为高可信度的因果模型。

## 生命周期

模型状态使用以下五类：

- `候选`：结构初步成立，证据不足。
- `活跃`：当前具有解释或预测价值。
- `争议`：存在重要冲突证据或竞争解释。
- `已合并`：已被更高解释力模型吸收。
- `已废弃`：核心机制被证伪或已无使用价值。

删除模型时保留演化记录；通常将正文标记为“已合并”或“已废弃”，而不是抹除历史。

## 结构一致性

- 每个正式模型只有一个主文件和一个主归属地图。
- 跨领域影响通过连接索引表达，不复制全文。
- 文件名使用稳定、简短、可读的中文名称；模型 ID 使用 `WM-地图编号-序号`。
- 地图编号固定为 `00` 至 `08`；`99` 只记录系统演化，不承载领域模型。
- 新模型、状态变化、重要修正、合并与废弃必须进入演化日志。

## AI 的职责

AI 同时承担研究员、架构师、编辑、挑战者与地图管理员角色。每次讨论结束时，应判断地图归属和变更类型，按统一结构输出，并实际维护相关索引。若发现模型可合并、框架有漏洞或跨领域模型能提高解释力，应主动提出并说明理由。

